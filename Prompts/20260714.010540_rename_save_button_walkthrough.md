# User Prompt
rename 'SAVE AND EXIT' to 'SAVE AND CLOSE'

# Walkthrough: Renaming Save Button

This document outlines the minor text update applied to the save button in the Settings menu.

## Actions Taken
1. **Located UI Element:** Found the `btnSaveSettings` button in `app/src/main/res/layout/activity_settings.xml`.
2. **Text Update:** Changed the hardcoded string value from `Save and Exit` to `Save and Close`. 

The button at the bottom of the Settings screen will now correctly read "Save and Close" to better reflect the action of closing the settings menu and returning to the game.
