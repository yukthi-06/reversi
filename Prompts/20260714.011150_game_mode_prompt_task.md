# User Prompt
when a new game is chosen, it should ask whether it is between two users and between user and AI(Computer).  and play should proceed accordingly

# Task Checklist
- [x] Edit `MainActivity.java` to intercept the `btnNew` click event.
- [x] Present an `AlertDialog` using `AlertDialog.Builder`.
- [x] Present two options: "Human vs Human" and "Human vs Computer".
- [x] Write the result to `SettingsManager.getInstance().aiEnabled` and call `save()`.
- [x] Launch `GameActivity` only after a selection is made.
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
