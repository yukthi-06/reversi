# User Prompt
make the black and while coins look like 3D.  Add some shading.

# Task Checklist
- [x] Edit `BoardView.java` to apply `RadialGradient` shaders to `paintBlack` and `paintWhite`.
- [x] Offset the center of the gradient to create a specular highlight (light source illusion).
- [x] Use `Color.parseColor` for subtle grayscale gradients representing the shading.
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
