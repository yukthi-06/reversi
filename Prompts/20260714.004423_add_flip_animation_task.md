# User Prompt
when computer plays it is very quick. slow it down a bit.
Also flipping of coins should be in animated fashion.

# Task Checklist
- [x] Identify where the AI turn occurs in `GameManager.java` and inject an artificial `Thread.sleep` to delay its execution and make the computer seem more deliberate.
- [x] Add an `animateFlips` method to `BoardView.java` that uses a `ValueAnimator` over 400ms to drive an animation loop.
- [x] Modify `BoardView.onDraw` to check if a specific coin is currently being flipped.
- [x] Apply a dynamic `canvas.scale(scaleX, 1f, cx, cy)` horizontally on the flipping coin to simulate a 3D spin on its Y-axis, transitioning from the old color to the new color exactly halfway through the animation.
- [x] Update `GameActivity.java` to call `boardView.animateFlips` instead of a static `invalidate()`.
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
