# User Prompt
the popup for selection is no good.  Create a new screen for this selection with the app theme

# Task Checklist
- [x] Create `app/src/main/res/layout/activity_mode_selection.xml`.
- [x] Style the layout using the app's native theme, including edge-to-edge support (`fitsSystemWindows`), appropriate padding, and large accessible buttons.
- [x] Create `app/src/main/java/com/vypeensoft/reversi/activity/ModeSelectionActivity.java`.
- [x] Implement button click listeners to update the `aiEnabled` flag in `SettingsManager`, save the configuration, launch `GameActivity`, and `finish()` the selection screen.
- [x] Update `MainActivity.java` to remove the old `AlertDialog` logic and instead route to the new `ModeSelectionActivity`.
- [x] Register the new activity inside `AndroidManifest.xml`.
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
