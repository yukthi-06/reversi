# User Prompt
when game is waiting on BLACK to play, the message says 'White's turn'... is that right?

# Task Checklist
- [x] Analyze `GameManager.java` specifically regarding the `updateScores()` invocation during `playMove()`.
- [x] Identify the bug: `updateScores()` was being called *before* `state.currentPlayer` was flipped to the opponent, causing it to read out the player who just finished their turn.
- [x] Relocate `updateScores()` to the bottom of the logic tree so it executes *after* the turn swaps (or passes, or ends).
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
