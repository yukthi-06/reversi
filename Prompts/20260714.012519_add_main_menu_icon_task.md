# User Prompt
in the main screen above the word "REVERSI" place the app icon in a decent size

# Task Checklist
- [x] Edit `app/src/main/res/layout/activity_main.xml`.
- [x] Insert an `ImageView` above the main title `TextView`.
- [x] Set the `android:src` to `@mipmap/ic_launcher` to load the application's icon.
- [x] Set the dimensions to a visually decent size (128dp by 128dp) with a small bottom margin so it doesn't touch the text.
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
