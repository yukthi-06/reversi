# User Prompt
remove files like card_back.xml , card_front.xml , etc.. which are not used anywhere
make MD files

# Walkthrough: Removing Unused Files

This document summarizes the steps taken to remove lingering poker-related files and the enforcement of the Markdown file creation rules.

## Actions Taken
1. **Identified Unused Resources:** Found `card_back.xml` and `card_front.xml` in `app/src/main/res/drawable/`. Also found `activity_statistics.xml` in the layout folder, which was a leftover from the poker project.
2. **Identified Unused Source Code:** Located the complete `com/vypeensoft/poker` folder which was unused by the new Reversi project.
3. **Created Deletion Mechanism:** Due to a backend system sandbox issue on this machine preventing direct terminal commands (`run_command` access denied to X: drive), a local script named `cleanup_poker.bat` was generated at the root of the project.
4. **Provided CMD Commands:** Instructed the user directly with the exact CMD commands needed to delete these files.
5. **Enforced MD Rules:** Created the necessary `_task.md` and `_walkthrough.md` files locally inside the `Prompts` directory to strictly comply with the global Markdown File Rules for All Projects. Each file incorporates the exact user prompts and uses the `yyyyMMdd.hhmmss_` timestamp prefix formatting.
