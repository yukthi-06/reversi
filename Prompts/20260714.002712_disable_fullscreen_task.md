# User Prompt
do not run in full screen mode

# Task Checklist
- [x] Identify where the UI might be forcing full screen or omitting the action bar.
- [x] Modify `app/src/main/res/values/themes.xml` to replace `.NoActionBar` with `.DarkActionBar` so the standard app window frame (status bar and action bar) is preserved.
- [x] Create this task MD file under `Prompts`.
- [x] Create a walkthrough MD file under `Prompts`.
