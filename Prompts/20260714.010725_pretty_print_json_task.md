# User Prompt
when writing to settings.json, make it pretty print

# Task Checklist
- [x] Identify where the JSON is written to disk (`SettingsManager.java` -> `save()`).
- [x] Replace the default `new Gson()` instance with a configured instance using `new GsonBuilder().setPrettyPrinting().create()`.
- [x] Ensure the output to `/sdcard/Vypeensoft/Reversi/settings/settings.json` is now neatly indented and formatted.
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
