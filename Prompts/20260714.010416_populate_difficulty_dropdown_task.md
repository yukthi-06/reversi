# User Prompt
difficulty dropdown is empty in settings

# Task Checklist
- [x] Identify missing binding logic in `SettingsActivity.java`.
- [x] Create a `String[]` array of difficulty levels: `["EASY", "MEDIUM", "HARD"]`.
- [x] Instantiate an `ArrayAdapter` with standard Android spinner layout resources and attach it to `spinnerDifficulty`.
- [x] Pre-select the spinner value based on `SettingsManager.getInstance().difficulty`.
- [x] Add an `OnItemSelectedListener` to save the newly selected difficulty back to the `SettingsManager` state when changed.
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
