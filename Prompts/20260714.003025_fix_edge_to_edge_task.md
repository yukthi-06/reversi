# User Prompt
the game screen is full screen

# Task Checklist
- [x] Identify the root cause for the layout drawing over the entire screen on API 35 (Edge-to-Edge default behavior).
- [x] Add `android:fitsSystemWindows="true"` to `activity_game.xml` root layout to ensure the content does not draw underneath system bars.
- [x] Add `android:fitsSystemWindows="true"` to `activity_main.xml` for consistency.
- [x] Create this task MD file under `Prompts`.
- [x] Create a walkthrough MD file under `Prompts`.
