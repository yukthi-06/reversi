# User Prompt
have a setting in Settings screen to select between the spherical coin of the flat coin.
Save all settings in a json file in /sdcard/Vypeensoft/Reversi/settings/settings.json

# Task Checklist
- [x] Edit `SettingsManager.java` to add `public boolean use3dCoins = false;`.
- [x] Create `SettingsActivity.java` to wire up the layout UI to the settings properties.
- [x] Add a `Switch` in `activity_settings.xml` called "Use 3D Spherical Coins".
- [x] Update `MainActivity.java` to launch `SettingsActivity` when the Settings button is pressed.
- [x] Modify `BoardView.drawCoin` to render flat or spherical based on `SettingsManager.getInstance().use3dCoins`.
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
