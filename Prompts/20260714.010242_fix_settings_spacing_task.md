# User Prompt
settings page fields are stuck to the top, bring it a bit down.

# Task Checklist
- [x] Edit `app/src/main/res/layout/activity_settings.xml`.
- [x] Add `android:fitsSystemWindows="true"` to the root `LinearLayout` so the layout stops drawing underneath the transparent Android 15 status bar.
- [x] Increase the root padding from `16dp` to `32dp` to give it a bit more breathing room at the top.
- [x] Create this task MD file in the `Prompts` directory.
- [x] Create the walkthrough MD file in the `Prompts` directory.
